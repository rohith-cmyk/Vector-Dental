# Docker Compose - optional; used for frontend build args when running docker compose
# Copy to .env if needed. Prefer using frontend/.env.local and frontend-gd/.env.local (see scripts/docker-build.sh)

# Backend API URL (as seen from the browser)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Supabase - required for frontend Docker builds if .env.local is missing
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# For full env documentation, see:
# - backend/.env.example
# - frontend/.env.example
# - frontend-gd/.env.example
# - caddy.env.example (for Caddy reverse proxy domains)
