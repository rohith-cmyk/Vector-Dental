# Caddy domain configuration
# Local development (default)
API_DOMAIN=api.localhost
APP_DOMAIN=app.localhost
GD_DOMAIN=gd.localhost

# Production - override when deploying:
# API_DOMAIN=api.yourdomain.com
# APP_DOMAIN=app.yourdomain.com
# GD_DOMAIN=gd.yourdomain.com

# When using Caddy, ensure backend/.env CORS_ORIGIN includes these domains:
# Local: http://app.localhost,http://gd.localhost,https://app.localhost,https://gd.localhost
# Production: https://app.yourdomain.com,https://gd.yourdomain.com
